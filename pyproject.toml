[project]
name = "ConsoleRPG"
version = "0.1.0"
description = "Config-driven console RPG prototype"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
  "typer>=0.20.1",
]

[project.optional-dependencies]
dev = [
  "ruff>=0.6.0",
]

[tool.pyright]
ignore = ["*"]
